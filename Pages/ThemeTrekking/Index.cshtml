@page
@model WEB315_Trekking_Locations_Asg_01.Pages_ThemeTrekking.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThemeTrekking[0].LocationName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeTrekking[0].PlanningToVisit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeTrekking[0].Place)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeTrekking[0].State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeTrekking[0].Country)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ThemeTrekking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocationName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlanningToVisit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Place)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
